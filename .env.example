# Absolute path to Brain2.0 Website/ folder (Obsidian Sync vault).
# Used by `npm run dev` and `npm run sync:content`.
# Leave unset on Vercel so the repo's ./content mirror is used.
CONTENT_DIR=/home/tracxn-lp-703/Obsidian Vault/Website

# Optional: canonical site URL for sitemap / Open Graph in production
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
